import AvailableSchedules from "./AvailableSchedules";

// Live Zoom Cohort admin page reusing the AvailableSchedules UI in "live" mode
const LiveZoomCohort = () => {
  return <AvailableSchedules mode="live" />;
};

export default LiveZoomCohort;
