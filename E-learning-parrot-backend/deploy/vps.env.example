VPS_HOST=root@YOUR_VPS_IP
VPS_PATH=/opt/e-learning-xander
# Set to 1 to import deploy/db/latest.sql.gz on next remote deploy
IMPORT_DB=1
# Password SSH (preferred on Windows). Never commit vps.env.
VPS_PASSWORD=
# Optional SSH key instead of password:
# SSH_KEY=C:\Users\user\.ssh\id_rsa
